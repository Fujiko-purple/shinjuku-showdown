# ============================================================================
# 仓库忽略清单
# 目的：只把「源码 + 构建脚本 + 文档 + 可部署产物」推到 GitHub，
#      把过程性大文件（截图、下载的二进制、临时构建）留在本地。
# ============================================================================

# 过程性截图：454 张 PNG，体积远大于源码本身，且是本地验收证据、不是交付物
shots/

# 美术参考原图（35 张漫画/动画扫描，13.5MB）：只留本地供建模对照。
# 来源清单已经写进 research/E-manga-art-refs.md（那份是纯文字，可以公开）。
reference/
_tools/.wc-build/

# 下载的第三方二进制（52MB）：任何人需要时用 deploy/README 里的命令自己下
deploy/bin/

# 可随时重建的冗余产物：zip 与测试夹具（docs/ 已经包含同样内容，重复推只会撑大仓库）
dist/shinjuku-site.zip
dist/_fixture.html

# 构建中间产物
node_modules/
*.log
tmp/

# 编辑器 / 系统
.vscode/
.idea/
Thumbs.db
desktop.ini
